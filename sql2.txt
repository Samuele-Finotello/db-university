Query con JOIN:

1 - SELECT students.`id`, students.`name`, students.`surname`, students.`registration_number`, students.`enrolment_date`, 
    degrees.  `name`
    FROM db_university.students
    INNER JOIN degrees ON students.degree_id = degrees.id
    WHERE degrees.`name` = "Corso di Laurea in Economia"

2 - 

3 - 

4 - 

5 - 

6 - 

7(bonus) - 